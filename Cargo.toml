[package]
name = "wisp"
version = "0.1.0"
edition = "2024"

[dependencies]
rustyline = "14"
macroquad = { version = "0.4", features = ["audio"] }
tiled = "0.12"
futures = { version = "0.3", default-features = false, features = ["executor"] }
